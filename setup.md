### Using Walnut a simple application framework:

```bash
git submodule add -b dev https://github.com/StudioCherno/Walnut
```

As walnut uses other repositories as submodules we need to initialize them recursively

```bash
git submodule update --init --recursive
```

Using [Walnut - Chat](https://github.com/TheCherno/Walnut-Chat):

```md
https://github.com/TheCherno/Walnut-Chat
```
